import {Component} from '@angular/core';
import {FormControl} from '@angular/forms';

import {Observable} from 'rxjs';
import {map, startWith} from 'rxjs/operators';

/**
 * @title bool overview
 */
@Component({
  selector: 'bool-overview-example',
  templateUrl: 'bool-overview-example.html',
  styleUrls: ['bool-overview-example.css']
})
export class BoolOverviewExample {}
